# Ticket — Tests workflow flakes on identical docs-only trees

Filed 2026-08-27 08:15 by the autobuild check-in, executing the 00:15
entry's own rule (*"a ticket if it fires again"* — it fired). GitHub
issues are disabled on the fork (`gh api repos/ElliotWood/traycer-remote-mobile
--jq .has_issues` → `false`), so the ticket lives here, beside the ledger
whose pushes produced the evidence. Close it by deleting this file in the
fixing commit.

## The observation

Six consecutive pushes to `main` are each a single-file delta to
`docs/autobuild/unreconciled-checkin-entries.md` (verified with
`git diff-tree --name-only` on all six, not inferred from the commit
messages), so the code tree is identical across all six. Their Tests
runs alternate:

| Commit | Tests, attempt 1 | Failing jobs |
| --- | --- | --- |
| `2c5dc114` | ❌ | `desktop darwin + packaging` — 1 of 25 in `src/electron-main/ipc/__tests__/host-management-channel.test.ts` (*"returns installVersion's committed ok outcome when its post-pin registry projection rejects"*) — plus `traycer-clients-gui-app shard 2` |
| `7f0ee0ab` | ✅ | — |
| `244ef823` | ❌ (run 32957853364) | the same pair — and the shard-2 log, re-read 2026-08-29 20:15 from `attempts/1/jobs`, **does** name its file: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 21,882 ms, the identical 39 as `fff118e2d` below |
| `77d276159` | ✅ (run 32979891662) | — |
| `98bfd7e01` | ✅ (run 32980641738) | — |
| `9cb18d9b2` | ❌ (run 32999100333) | `traycer-clients-gui-app shard 4` — a **third** distinct job |
| `b21d05c00` | ❌ (run 33122275631) | `traycer-clients-gui-app shard 4` again — the family's first **repeat** member; `gh run rerun --failed` at 12:19 AEST, attempt 2 green (shard 4 passed 12:25:51 AEST). Filed by the 16:15 run; the 12:15 run that caught it ended before the rerun finished |
| `1d5f3b88d` | ❌ (run 33147777425) | `traycer-clients-gui-app shard 3` — a **fourth** distinct shard; the failed step's log again names no test (NX-collapsed, read not assumed). `gh run rerun --failed` at 20:18:45 AEST by the 20:15 run; attempt 2 green (shard 3 passed 20:24:16 AEST), read at landing time from `actions/runs/33147777425/attempts/2/jobs` |
| `f961c985e` | ❌ (run 33228761908) | `desktop darwin + packaging` — a **fifth** distinct member and a **second** darwin test: 1 of 25 in `src/electron-main/host/__tests__/host-lifecycle.test.ts` (*"forced reload emits null for unchanged unreachable pid metadata and restores the same host id when it is reachable again"*), named by the job log (darwin is not NX-collapsed). Upstream-inherited (authors Anurag Sharma / Hardik Shingala, last touched on `main` 2026-08-03 by #913; not in the fork's 545 since-base paths). All four gui-app shards green on attempt 1. `gh run rerun --failed` at 16:19:24 AEST by the 16:15 run; attempt 2 green (darwin passed 16:21:35 AEST), read by the 20:15 run from `actions/runs/33228761908/attempts/2/jobs` |
| `fff118e2d` | ❌ (run 33238440979) | `traycer-clients-gui-app shard 2` — the shard-2 member's second appearance, and the log names it: `src/components/settings/panels/__tests__/providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 22,172 ms — the **same 39 tests by name** as run 32957853364 (the two `×` lists diffed: identical; 13 over 1 s and 26 under, in both). The assertion text is cut by Nx's replay cap in both logs. Passes **74/74 locally** on this tree (`fff118e2d`, 103.9 s, 20:24 AEST). Upstream-inherited (Hardik Shingala / Anurag Sharma, last touched on `main` 2026-08-05 by #976; not in the fork's 545 since-base paths; upstream has moved it ten times since — blob `a341e87ca` ours vs `b1509b9d1` theirs — so the fork merge replaces it). All other 13 jobs green on attempt 1. `gh run rerun --failed` at 20:20:31 AEST by the 20:15 run; attempt 2 green (shard 2 passed 20:26:26 AEST), read at landing. A two-file docs delta (this ticket + the ledger), still no code. **2026-08-30 00:15:** the exact CI invocation (`vitest run --config vitest.config.ts --shard=2/4`) run locally in `wt-guiapp-main` at `b88160082` → **264 files / 2904 tests, all passed**, the same set as CI's shard 2, in 901.58 s (import 642.36 s vs CI's 170.04 s) — a run 3.5× slower than CI's passed all 74, so speed alone does not select the 39. |
| `2033ae2ba` | ❌ (run 33426074181) | `traycer-clients-gui-app shard 4` — the shard's **third** appearance, and the family's **first red under `--outputStyle=stream`**: the job log (job `99599830543`) names everything ask (1) said it would — `src/components/home/host-workspace-selector/__tests__/workspace-folders-refresh.test.tsx` > *"folder-mapping refresh affordance > re-derives on R while the picker is open"*, `AssertionError: expected +0 to be 1 // Object.is equality` with the expected/received diff and the code frame at `:117:7` (`fireEvent.keyDown(document.body, { key: "r" })` … `.toBe(1)` — a listener-registration race by shape), plus the full summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 1 failed \| 2638 passed (2639)`, `Duration 304.29s (import 243.25s, tests 101.04s)` — **80% of the red shard is module import** on the 2-core box. Upstream-inherited (Hardik Shingala, #852/#878; fork last touched it 2026-08-01; upstream has rewritten it three times since — #1188, #1310, #1514 — so the fork merge replaces this file too). Not in the merge map's 51. All other 13 jobs green on attempt 1; a one-file docs delta (the 04:15 ledger entry). `gh run rerun --failed` at 08:25:12 AEST by the 08:15 run; attempt 2 green (shard 4 passed 08:29:16 AEST, 3 m 53 s), read by the same run from `attempts/2/jobs` |
| `8750f8db4` | ❌ (run 33463226096) | `traycer-clients-gui-app shard 2` — the shard-2 member's **third** appearance and its first under `--outputStyle=stream`: `src/components/settings/panels/__tests__/providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 21,989 ms — the **same 39 tests by name** as runs 32957853364 and 33238440979 (the `×` list, timings stripped, diffed against the saved `fff118e2d` list: identical). The stream log carries what the replay cap cut twice: the first failure, *"edits and switches the default account"* (1,378 ms), is `AssertionError: expected "vi.fn()" to be called at least once`, and its DOM dump shows `<body data-scroll-locked="1" style="pointer-events: none;">`, a `data-radix-focus-guard` span and the panel's `<header aria-hidden="true">` — a Radix overlay still mounted over the panel — after which the remaining 38 read as its wake: 22 × `TestingLibraryElementError: Unable to find an accessible element with the role "button" and name …`, 13 × `waitFor` timeouts, 10 × *"expected vi.fn() to be called at least once"*, 3 × *"called 1 times, but got 0 times"*, 2 × *"Unable to find a label with the text of: Profile name"* (counted per line; a block can carry both a `waitFor` frame and its assertion). Shape, not proof: a dialog one test opened and did not close, and every later test querying a tree it cannot reach — ask (2)'s first concrete target. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 318.48s (import 217.63s, tests 174.45s, environment 180.73s)` — import 68 % of wall, against 80 % / 81 % on the two shard-4 readings. Blob unchanged since the row above (`a341e87ca` ours / `b1509b9d1` theirs; 16 upstream commits since the merge-base, 0 this window). A one-file docs delta (the 12:15 ledger entry); all other 13 jobs green on attempt 1. `gh run rerun --failed` at 16:19:54 AEST by the 16:15 run; attempt 2 green (shard 2 job `99758684448` passed 16:24:49 AEST, 4 m 49 s), read by the same run from `attempts/2/jobs` |
| `ad0a7e38d` | ❌ (run 33520784905) | `desktop darwin + packaging` — **not a test failure, and not a member: the job never acquired a runner.** From `attempts/1/jobs` (job `99899196641`): created 00:38:54 AEST, `runner_name` empty, `steps` 0, log URL **404 `BlobNotFound`** (no log was ever written), conclusion **`cancelled`** at 00:53:57 (**15 m 3 s**), check-run annotation *"The job was not acquired by Runner of type hosted even after multiple attempts"*. Control in the same run's neighbour: the *Real supervisor* workflow's `CLI real-launchd rows (macOS)` job on the same tip and the same `macos-latest` label, created the same second (00:38:54), acquired `GitHub Actions 1000004788` in **5 s** and passed at 00:39:59 — hosted macOS capacity existed; one of two identical-label jobs was never assigned. All thirteen ubuntu jobs green on attempt 1 (23 s – 5 m 21 s). `githubstatus.com`: no incident over 00:38–00:54 AEST (Actions `operational`). The `queued-job-reads-as-cancelled` shape at fifteen minutes instead of twenty-four hours. `gh run rerun --failed` at 04:20:38 AEST by the 04:15 run; attempt 2 acquired `GitHub Actions 1000004808` in **3 s** and went green (darwin job `99973819815` 04:20:43 → 04:23:54, 3 m 11 s; run `success` 04:23:55), read by the same run from `attempts/2/jobs`. A one-file docs delta (the 00:15 ledger entry) |
| `d1cf952c8` | ❌ (run 33940178078) | `traycer-clients-gui-app shard 2` — the shard-2 member's **fourth** appearance and its second under stream: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 19,680 ms — the **same 39 by name** (× list, timings stripped, sorted-diffed against the saved 09-01 list: identical), first failure *"edits and switches the default account"* (1,287 ms, `expected "vi.fn()" to be called at least once`) with the same Radix overlay DOM (`data-scroll-locked="1"`, `pointer-events: none`), the other 38 its wake. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 258.11s (import 175.42s)` — import 68 % of wall, in family range. Ours' blob unchanged (`a341e87ca` — four reds on identical bytes); upstream's copy has moved again (`f78fbd45f` at `a87f530e4`), so the fork merge still replaces it. A one-file docs delta (the 12:15 ledger entry); all other 13 jobs green on attempt 1. `gh run rerun --failed` at 16:18 AEST by the 16:15 run; attempt 2 green (shard 2 job `101261398063` 16:18:57 → 16:24:44, 5 m 47 s), read by the same run from `attempts/2/jobs` |
| `36803293d` | ❌ (run 33960887230) | `traycer-clients-gui-app shard 2` — the shard-2 member's **fifth** appearance and its third under stream: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 22,553 ms — the **same 39 by name** (FAIL-line list, sorted-diffed against the saved 09-01 list: identical), first failure *"edits and switches the default account"* (1,414 ms, `expected "vi.fn()" to be called at least once`) with the same Radix overlay DOM (`data-scroll-locked="1"`, `pointer-events: none`), the other 38 its wake. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 335.53s (import 230.06s)` — import 69 % of wall, in family range. Ours' blob unchanged (`a341e87ca` — five reds on identical bytes); upstream's copy sat still this window (`f78fbd45f` at both `696c694fa` and `c3f733113`), so the fork merge still replaces it. A one-file docs delta (the 20:15 ledger entry); all other 13 jobs green on attempt 1. `gh run rerun --failed` at 00:22:36 AEST by the 00:15 run; attempt 2 green (shard 2 job `101321052207` 00:22:41 → 00:26:44, 4 m 3 s; run success 00:26:45), read by the same run from `attempts/2/jobs` |
| `f3a107b72` | ❌ (run 34016639301) | `traycer-clients-gui-app shard 2` — the shard-2 member's **sixth** appearance and its fourth under stream: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) — the **same 39 by name** (FAIL-line list from the stream log, sorted-diffed against the saved 09-06 16:15 list after line-ending normalisation: identical — both normalise to git blob `7470fb701`), first failure *"edits and switches the default account"* (1,435 ms) with the same Radix overlay DOM (`data-scroll-locked="1"`), the other 38 its wake. Summary: `Test Files 1 failed \| 263 passed (264)`, `Duration 324.32s (import 220.65s, tests 178.17s, environment 184.05s)` — import 68 % of wall, in family range. Ours' blob unchanged (`a341e87ca` — six reds on identical bytes); upstream's copy sat still this window (`f78fbd45f` at `4fa18be1f`), so the fork merge still replaces it. A two-commit, one-file docs delta (the posthumous 08:15 entry + the 16:15 entry); all other 13 jobs green on attempt 1 (shard 2 red 16:30:03 → 16:35:51 AEST). `gh run rerun --failed` at 00:26:18 AEST **by the 00:15 run, nine minutes before the box slept under it** — the run was reaped without recording the rerun it fired; attempt 2 green (shard 2 job `101502636355` 00:26:22 → 00:31:01, 4 m 39 s; run success 00:31:02), read by the 10:01 catch-up run from `attempts/2/jobs` |
| `14f6779b8` | ❌ (run 34069518595) | `traycer-clients-gui-app shard 2` — the shard-2 member's **seventh** appearance and its fifth under stream: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 22,395 ms — the **same 39 by name** (FAIL-line list from the stream log, sorted-diffed against the saved 09-07 00:15 list after line-ending normalisation: identical — both normalise to git blob `7470fb701`), first failure *"edits and switches the default account"* (1,424 ms) with the same Radix overlay DOM (`data-scroll-locked="1"`), the other 38 its wake. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 336.06s (import 230.67s, tests 182.13s, environment 190.99s)` — import 69 % of wall, in family range. Ours' blob unchanged (`a341e87ca` — seven reds on identical bytes); upstream's copy sat still this window (`f78fbd45f` at `011293a54`), so the fork merge still replaces it. A two-file docs delta (the 10:01 catch-up entry + its flake row); all other 13 jobs green on attempt 1. `gh run rerun --failed` at 12:19 AEST by the 12:15 run; attempt 2 green (shard 2 job `101601855494` 12:19:37 → 12:25:22 AEST, 5 m 45 s; run success 12:25:22), read by the same run from `attempts/2/jobs` |
| `71d36bcde` | ❌ (run 34076549255) | `traycer-clients-gui-app shard 2` — the shard-2 member's **eighth** appearance and its sixth under stream: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 22,323 ms — the **same 39 by name** (× list from the API job log — project prefix and timings stripped by a corrected extractor (`extract-shard2-0908.py`; the saved stream-format one reads 0 from API logs), sorted-diffed against the saved 09-07 12:15 list after line-ending normalisation: identical — both normalise to git blob `7470fb701`), first failure *"edits and switches the default account"* (1,391 ms) with the same Radix overlay DOM (`data-scroll-locked="1"`), the other 38 its wake. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 333.85s (import 227.98s, tests 181.41s, environment 190.18s)` — import 68 % of wall, in family range. Ours' blob unchanged (`a341e87ca` — eight reds on identical bytes); upstream's copy sat still across both window tips (`f78fbd45f` at `011293a54` and `3529b18a9`), so the fork merge still replaces it. A two-file docs delta (the 12:15 entry + its flake row); all other 13 jobs green on attempt 1 (shard 2 red 12:31:10 → 12:37:14 AEST 09-07). The red then sat **31.9 hours unanswered** — the box slept at 13:00:13, five check-in windows never fired and the wake got no catch-up; `gh api …/rerun-failed-jobs` at 20:23:42 AEST 09-08 by the 20:15 catch-up run; attempt 2 green (shard 2 job `102025249523` 20:23:47 → 20:29:42 AEST, 5 m 55 s; run success 20:29:43), read by the same run from `attempts/2/jobs` |
| `6d267111e` | ❌ (run 34238952649) | `traycer-clients-gui-app` — **the unnumbered shard-1 job's first appearance in this table** (ninth red row; every prior gui-app red was shards 2–4): `src/components/layout/dialogs/desktop/__tests__/logs-chooser-dialog.test.tsx` — **1 failed** — *"gates the failed-snapshot report action on capability and never forwards the raw error"* (× at 158 ms), `TestingLibraryElementError: Unable to find an accessible element with the role "button" and name "Report issue"`; the accessible-roles dump shows the dialog WAS mounted — title *"Open Logs"*, two Close buttons, sr-only description — so the capability-gated content had not arrived when the sync `getByRole` ran. Summary: `Test Files 1 failed \| 262 passed \| 1 skipped (264)`, `Tests 1 failed \| 2812 passed \| 1 skipped (2814)`, `Duration 205.46s (transform 22.64s, setup 16.27s, import 180.89s, tests 67.72s, environment 118.04s)` — import **88 %** of wall, past the family's 68–81 % range on the same 2-core mechanism. Tree vs the all-green `7304d35e7` (00:15's attempt-1 green, all six workflows): the 00:15 ledger entry plus the `scripts/autobuild-checkin.ps1` cherry-pick — zero bytes under `clients/` — a nondiscriminating red on identical code (docs-only push). Upstream-inherited (#847 Anurag Sharma 2026-07-31 / #307 Hardik Shingala; the fork has never touched it), and upstream has since moved BOTH blobs (test `f098aec0b` ours vs `0071ca306` theirs; component `a3fea17bb` vs `4cadbbbde`), so the fork merge replaces the pair. Not in the merge map's 54. All other 13 jobs green on attempt 1, shard 2 included (shard-1 red 00:32:52 → 00:36:43 AEST 09-09). `gh run rerun --failed` at 04:19 AEST by the 04:15 run; attempt 2 green (shard-1 job `102183259026` 04:19:46 → 04:25:10 AEST, 5 m 24 s; run success 04:25:11), read by the same run from `attempts/2/jobs`. A two-file docs delta (the 04:15 ledger entry + this row) |
| `e3d1c4067` | ❌ (run 34982185043) | `test (@traycer-clients/desktop)` — **a sixth distinct member, and the family's first red outside darwin and the gui-app shards**: `clients/desktop/src/electron-main/auth/__tests__/file-token-store.test.ts` > *"FileTokenStore (real fs + lock/WAL) > subscribe is a live registration that never fires (§4 stub)"*, `AssertionError: expected 1 to be +0 // Object.is equality` at `:377:19` (× at 250 ms) — the test registers a subscriber, does a `signIn` then a `rotate`, disposes, and asserts `fired` is **0**; it read **1**, so the subscription fired once. Summary: `Test Files 1 failed \| 96 passed (97)`, `Tests 1 failed \| 1439 passed \| 3 skipped (1443)`, `Duration 34.14s (transform 3.46s, setup 1.21s, import 8.43s, tests 30.76s, environment 50.32s)` — import **25 %** of wall, far outside the gui-app family's 68–88 % range, so this member is not the 2-core import-pressure shape; the job failed in **64 s** (14:31:13Z → 14:32:17Z) where every gui-app member takes 3–6 minutes. All other 13 jobs green on attempt 1, **all four gui-app shards included**. Provenance follows the family rule exactly and then some: the file is **not** in the fork's 546 since-base paths and **not** in the merge map's 61, and `ours` == `base` == `0553b7046` — *the fork has never touched it since the merge-base* (its last commit, `5e1b3802b` #811, is inherited). Upstream has moved it **five** times since (`theirs 5cbafc080`: #889, #1462, #572, #1123, #1101), and **#889 (*Local-first room backend*) deleted this test outright** — `subscribe` became a real live registration, so the "never fires (§4 stub)" assertion no longer exists upstream (grepped at `685e5bae7`: **absent**). The fork is asserting stub behaviour on a subscribe path that upstream has since made live, which is both why it can race and why **the fork merge is the deflake for this member too** — by deletion rather than by rewrite. A one-file docs delta (the 00:15 ledger entry); `gh run rerun --failed` by the 00:15 run, attempt 2 green (att 1 14:31:09Z, att 2 14:38:23Z; run `success`), read here by the 12:15 run from the attempts API — the row itself was owed forward because the 00:15 entry had already landed when the red was diagnosed |
| `877d9fff2` | ❌ (run 35064299770, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 2` — the shard-2 member's **ninth** appearance: `providers-settings-panel.test.tsx` (74 tests \| **39 failed**) in 22,079 ms, the same 39, on a docs-only push. Recorded by the 2026-09-16 20:15 check-in |
| `a39066030` | ❌ (run 35107909192, attempt 1; attempt 2 green) | `desktop darwin + packaging`: 1 failed of 97 files in `src/electron-main/host/__tests__/host-lifecycle.test.ts` (*"forced reload emits null for unchanged unreachable pid metadata and restores the same host id when it is reachable again"*, got `{ hostId: 'same-host', … }` where it expected null). This is the darwin host-lifecycle member's **second** appearance (the first was `f961c985e`), on a docs-only tree |
| `aadb035c8` | ❌ (run 35210365833, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 2` — the shard-2 member's **tenth** appearance: `providers-settings-panel.test.tsx`, `Tests 39 failed \| 2865 passed (2904)`, first failure *"edits and switches the default account"* over the same Radix overlay DOM, on a docs-only push; ours' blob still `a341e87ca`. `gh run rerun --failed` 00:16:30 AEST; shard 2 job `105239855774` green 00:16:35 → 00:22:15. Recorded by the 2026-09-18 00:15 check-in |
| `1a2a6a15d` | ❌ (run 35233499319, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 3` — **a seventh distinct member, and the shard-1 member's twin**: `src/components/layout/dialogs/desktop/__tests__/about-details-dialog.test.tsx` > *"gates the failed-snapshot report action on capability and never forwards the raw error"* (× at 264 ms of `2 tests \| 1 failed`), `TestingLibraryElementError: Unable to find an accessible element with the role "button" and name "Report issue"` at `:54:28` — the **same test title, same assertion and same provenance pair** as the shard-1 member in the `6d267111e` row (`logs-chooser-dialog.test.tsx`), so the two are one shape on two shards. Summary: `Test Files 1 failed \| 263 passed (264)`, `Tests 1 failed \| 2510 passed (2511)`, `Duration 258.97s (transform 19.42s, setup 19.27s, import 193.76s, tests 129.56s, environment 142.70s)` — import **75 %** of wall, inside the family's 68–88 % range. **The diagnosis moves past the `6d267111e` guess.** That row said *"the capability-gated content had not arrived when the sync `getByRole` ran"*; the accessible-roles dump names the step. It lists exactly one `paragraph` (the `DialogDescription`, `max-w-[72ch] … *:[a]:underline`) plus two `Close` buttons — and the `unavailable` branch renders `<div><span>{message}</span><ReportIssueAction/></div>`, a **roleless** span, where the `loading` branch would add a second `<p>`. One paragraph therefore proves the awaited `getSnapshot` rejection **had** settled and its message was on screen; the only thing missing was `ReportIssueAction`, whose whole gate is the synchronous `if (!reportIssueAvailable) return null`. The failing step is thus `act(() => setState({ reportIssueAvailable: true }))` **not having rendered** — not the async snapshot. Four mechanisms eliminated by reading the tree rather than by rerunning: (1) nothing else can clobber the flag — all 19 non-test references to `reportIssueAvailable` are reads; (2) cross-file store leakage is impossible — `clients/gui-app/vitest.config.ts` sets `pool: "forks"` and leaves `isolate` at its default `true`; (3) no Radix async naming — this call site passes `presentation="link"`, a plain `<Button>Report issue</Button>`, not the tooltip-wrapped `icon` arm whose name comes from `aria-label`; (4) no async rehydrate — the store is a bare `create(...)` with no `persist` middleware. What remains is the act-environment / `useSyncExternalStore` flush itself, which is also why the family selects these two sibling dialog tests and nothing else — the first concrete target ask (2) has had since the `8750f8db4` row. Provenance follows the family rule to the letter: **`ours` == `base`** for both blobs (test `b9e32dc1c`, component `191147e90`) — *the fork has never touched either since the merge-base* — their last touches are the same two upstream PRs as the twin (#847 Anurag Sharma 2026-07-31; #307 Hardik Shingala 2026-07-12), upstream has since moved **both** (test `ed662a807`, component `40e16c1af`), and neither path is in the merge map's 61. **So the fork merge is the deflake for this member too.** All other 13 jobs green on attempt 1 (shard 3 red 14:25:54Z → 14:30:38Z). `gh run rerun --failed` at 04:18:36 AEST by the 04:15 run; attempt 2 green (shard 3 job `105326481659` 18:18:43Z → 18:24:16Z, 5 m 33 s), read by the same run from `attempts/2/jobs`. A two-file docs delta (the 00:15 ledger entry + its flake row) |
| `162a6421d` | ❌ (run 35282302961, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 2` — the shard-2 member's **eleventh** appearance, and **the first red in this family to outlive its own check-in window**. `providers-settings-panel.test.tsx`, `Tests 39 failed \| 2865 passed (2904)`, `Test Files 1 failed \| 263 passed (264)`, `Duration 319.09s (transform 19.08s, setup 25.70s, import 217.73s, tests 173.98s, environment 181.65s)` — import **68 %** of wall, the bottom of the family's 68–88 % band. First failure *"edits and switches the default account"* (1,390 ms) over the same Radix overlay DOM as the tenth; the 39 break down as **50 `TestingLibraryElementError: Unable to find …`** and **26 `AssertionError`** across the file. Provenance re-derived, not carried, and it still satisfies the family rule exactly: **`ours` == `base` == `a341e87ca`** (the fork has never touched this file since the merge-base), `theirs` is now **`46bd4f3bb`** — upstream has moved it *again* since the 2026-09-18 row recorded `b1509b9d1` — and the path is **not among the merge map's 61**, so the fork merge remains the deflake. **Why it sat red for 25.7 h.** Its own window (2026-09-18 08:15) pushed twice: `9859664af`, then the correction `162a6421d` 48 s later. The second push **cancelled the first's Tests** (run `35282239763`, `conclusion: cancelled`) — the concurrency-by-ref trap, now on its fourth face — and the window ended while `162a6421d`'s own Tests were still running, so it recorded era-58 (`de3e547fe`) green and never saw this. The next four windows were **rate-limited** and read nothing. `gh run rerun --failed` at 2026-09-19 10:05 AEST by the 09:59 catch-up run, 25.7 h after the red; **attempt 2 green**, read at landing from `actions/runs/35282302961/attempts/2/jobs` — shard 2 job `00:05:33Z → 00:11:14Z`, 5 m 41 s, so the eleventh appearance reruns green like the ten before it. Tally moves to **59 / 44 / 15** (eras / Tests green on attempt 1 / Tests red on attempt 1); the partition still holds exactly (44 + 15 = 59) |
| `d232cf9e0` | ❌ (run 35408700158, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 3` — `about-details-dialog.test.tsx`, `Test Files 1 failed \| 263 passed (264)`, `Tests 1 failed \| 2510 passed (2511)`: the **second** appearance of the seventh member, first seen on `1a2a6a15d` four eras earlier, and the **same counts to the test** (1 of 2511) as that row. Caught in-window by the 09-19 09:59 run, which had just landed the era-59 row above — so the family produced **two reds in two consecutive eras on two different shards**, having gone green for era-58 in between. **This one cannot be blamed on the delta:** `d232cf9e0` changes four files — two `docs/autobuild/*.md` and two `scripts/*.ps1` — and **no TypeScript, no gui-app source, no config**, so the tree under test is identical to era-59's and the shard-3 red is the family, not the commit (`git diff-tree --no-commit-id --name-only -r d232cf9e0`, read rather than assumed). Provenance carries from the `1a2a6a15d` row unchanged: **`ours` == `base`** on both blobs, both last touched by upstream PRs, neither path among the merge map's 61 — the fork merge remains the deflake. Tally **60 / 44 / 16** (44 + 16 = 60). **The rerun this row implied was never issued.** The 09:59 window filed the row and ended; `actions/runs/35408700158` still read `run_attempt: 1`, `conclusion: failure` at **2026-09-19 12:23 AEST**, 12.1 h later, when the 12:15 window read it — so the row's missing "attempt 2 green" was a real gap, not a formatting omission, and reading it as "green on rerun by analogy" would have invented the measurement. `gh run rerun --failed` issued 12:23 by the 12:15 run; **attempt 2 green**, shard 3 job `02:24:56Z → 02:30:13Z` (5 m 17 s), all twelve jobs green, waited out in the foreground. **Convention this adds: a row filed without an attempt-2 outcome means the rerun state is UNKNOWN — the next window must read `run_attempt` before carrying it.** |
| `d8febc7e3` | ❌ (run 35426473121, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 3` — `about-details-dialog.test.tsx` > *"gates the failed-snapshot report action on capability and never forwards the raw error"*, `TestingLibraryElementError: Unable to find an accessible element with the role "button" and name "Report issue"` at `:54:28`: the **third** appearance of the seventh member, and its **second in four eras** (`1a2a6a15d`, `d232cf9e0`, here) with eras 61 and 62 green in between. `Test Files 1 failed \| 263 passed (264)`, `Duration 196.79s (transform 13.99s, setup 14.95s, import 139.03s, tests 106.97s, environment 106.62s)` — import **70.6 %** of wall, inside the family's 68–88 % band; the red shard took 3 m 38 s (06:23:38Z → 06:27:16Z), well under the family's usual 5–6 minutes, which is the import-pressure shape arriving early rather than a different failure. Delta is **one file** (`docs/autobuild/unreconciled-checkin-entries.md`, entry 98) — no TypeScript, no config — so the tree under test is identical to era-62's, which was green on all six. **The `d232cf9e0` diagnosis reproduces exactly, and this is the first time it has been checked twice.** That row argued from the accessible-roles dump that the *awaited* `getSnapshot` rejection had already settled and only `ReportIssueAction` was missing, so the failing step is `act(() => setState({ reportIssueAvailable: true }))` not having rendered — not the async snapshot. This dump says the same thing in the same three signals: exactly **one** `paragraph` (the sr-only `DialogDescription`, `max-w-[72ch] … *:[a]:underline`, text *"Desktop runtime and diagnostics details."*), exactly **two** `Close` buttons (footer + `data-slot="dialog-close"`), and the `unavailable` branch rendered as `<div class="grid gap-3"><div …><span>Could not load desktop details.</span></div></div>` — the roleless span, with **no `ReportIssueAction` sibling**. A loading branch would have added a second `<p>`; there is one. So the prediction held on a fresh instance: the rejection settles, the capability flag does not flush. Provenance carries unchanged and was re-checked, not assumed: **`ours` == `base`** on both blobs, both last touched by upstream PRs (#847 Anurag Sharma 2026-07-31, #307 Hardik Shingala 2026-07-12), upstream has moved both since, and neither path is among the merge map's 61 — **the fork merge remains the deflake**. All other 13 jobs green on attempt 1. `gh run rerun --failed` issued **16:30:20 AEST, in-window, 3 m 4 s after the red completed** — the era-60 convention (a filed row without an attempt-2 outcome means UNKNOWN) applied in advance by rerunning *before* writing the row; **attempt 2 green**, shard 3 job `06:30:26Z → 06:36:19Z` (**5 m 53 s**), all 14 jobs green, waited out in the foreground and read from `attempts/2/jobs`. Tally **63 / 46 / 17** (46 + 17 = 63) |
| `6b4c7b555` | ❌ (run 35437294714, attempt 1; attempt 2 green) | `traycer-clients-gui-app shard 2` — `providers-settings-panel.test.tsx`, the **eighth** appearance of the first-named member and **the eighth time it has failed exactly 39**: `Test Files 1 failed \| 263 passed (264)`, `Tests 39 failed \| 2865 passed (2904)`. That invariant is the identification — a suite whose totals have grown with upstream still loses the identical 39 of that file's 74, so the count is a property of the file under import pressure, not of how much else is in the shard. Failure shapes split **50 `TestingLibraryElementError` / 26 `AssertionError` on a `vi.fn()` never called**, the leading one being *"shows the Cancel button's pending state per the AGENTS.md recipe"* → `Unable to find an accessible element with the role "button" and name "Link account"` at `:3183:28`, with the accessible-roles dump showing the panel **rendered** (heading *"Providers"*, `Refresh providers` and `Filter providers` buttons present) and only the account rows missing — the same render-arrived-without-its-async-children shape the seventh member shows on shard 3, on a different component. **`Duration 229.87s (transform 15.09s, setup 15.89s, import 152.72s, tests 150.50s, environment 113.69s)` — import is 66.4 % of wall, which is BELOW the family's stated 68–88 % band**, recorded as measured rather than rounded into the band: the band is now 66–88 %, and if the next appearance lands lower again the band is drifting rather than describing. Red shard 10:24:05Z → 10:28:20Z (**4 m 15 s**), all other 13 jobs green on attempt 1. **Cannot be blamed on the delta:** `git diff-tree --no-commit-id --name-only -r 6b4c7b555` is **two files** — `docs/autobuild/unreconciled-checkin-entries.md` (entry 99) and `scripts/autobuild-checkin.ps1` (a prompt-only paragraph) — **no TypeScript, no gui-app source, no config**, so the tree under test is identical to era-64's, which was green on all six. Provenance re-checked, not carried: **`ours` == `base`** (`a341e87ca`) against `8f21d506f`, last touched by upstream PR **#976** (Hardik Shingala, 2026-08-05), and the path is **not among the merge map's 61** (`grep -c` on the 09-19 16:15 path set → **0**) — **the fork merge remains the deflake**. `gh run rerun --failed` issued **20:29:58 AEST, 1 m 38 s after the red completed**, ahead of writing this row as the era-63 convention now requires; **attempt 2 green**, shard 2 `10:30:02Z → 10:34:48Z` (**4 m 46 s**), all 14 jobs green, waited out in the foreground and read from `attempts/2/jobs`. Tally **65 / 47 / 18** (47 + 18 = 65) |
| `505243649` | ❌❌ (run 35448503170, attempt 1 **two shards**; attempt 2 **still red on shard 2**) | **Two family firsts in one era.** (1) `traycer-clients-gui-app shard 2` **and** `shard 3` both red in the **same run** — every one of the twenty-five rows above reads *"all other 13 jobs green on attempt 1"*, so two members failing together has never happened before. Shard 2 is `providers-settings-panel.test.tsx`, `Tests 39 failed \| 2865 passed (2904)`, `Duration 331.07s (transform 19.02s, setup 27.12s, import 226.05s, tests 180.44s, environment 188.14s)` — import **68.3 %**, in the 66–88 % band — with the usual `AssertionError: expected "vi.fn()" to be called at least once` and *"Unable to find a label with the text of: Profile name"*. Shard 3 is `about-details-dialog.test.tsx`, `Tests 1 failed \| 2510 passed (2511)`, `Duration 314.34s (… import 230.02s …)` — import **73.2 %** — with the same *"Unable to find an accessible element with the role `button` and name `Report issue`"* as its three prior appearances. Both are known members; what is new is their coincidence. (2) **The shard-2 member failed its rerun.** `gh run rerun --failed` issued **00:28:55 AEST, 1 min after the red completed**, per the era-63 convention; attempt 2 ran both shards, **shard 3 went green** (job `105912425436`, `14:28:58Z → 14:34:03Z`, 5 m 5 s) and **shard 2 went red again** (job `105912425429`, `14:28:58Z → 14:34:24Z`, 5 m 26 s) — same file, `Tests 39 failed \| 2865 passed (2904)`, `Duration 300.43s (… import 206.20s …)`, import **68.6 %**. **This falsifies the property the prose above has asserted since 2026-08-29** — *"green on every rerun and locally"* — which held across its first eight appearances. The **exactly-39** invariant did **not** break: it is now **ten consecutive** readings of 39 of that file's 74, across two different attempts of the same run. **Delta cannot be blamed:** `505243649` is two files, the ledger entry and a **new** `scripts/merge-map.py` — no TypeScript, no gui-app source, no config — so the tree under test is identical to era-66's (`ac96ee89e`), which was green on all six. Provenance unchanged and re-checked: **`ours` == `base`** for both members, both last touched by upstream PRs, neither path among the merge map's 61 (re-derived this window at `61 / 155`) — **the fork merge remains the deflake**. All other 12 jobs green on attempt 1. Tally **67 / 48 / 19** (48 + 19 = 67). **The question this row opened was answered in the same window, not carried.** It asked whether shard 2 had stopped being a flake and gone persistently red. Era-68 (`fac720f5f` — the shebang fix plus this row, a vitest-identical tree) came back **green on attempt 1 on all six workflows**, shard 2 included (`14:36:33Z → 14:40:41Z`, 4 m 08 s) and shard 3 with it (4 m 06 s). So **no**: the member is still a flake, and the tally's "red on attempt 1" column keeps its meaning. What era-67 changed is narrower and still stands — re-running the *same run* is no longer a reliable way to clear this member, while a **fresh run on the same tree** cleared it at once. Tally **68 / 49 / 19** (49 + 19 = 68) |

Identical trees, twenty-six red runs (the tenth with no test in it: a `macos-latest` job never acquired a runner and was cancelled by GitHub at fifteen minutes, rerun green; the twenty-sixth is the first to carry **two** red shards at once), seven distinct failing members (two darwin tests, three gui-app shards, one ubuntu desktop suite, one about-details dialog), shard 4 three times, shard 3 five times and shard 2 thirteen times — and four members are now **named**: shard 2's (the same 39 of 74 in `providers-settings-panel.test.tsx` on all **ten** of its readings — an invariant that has never once broken — but **no longer green on every rerun**: era-67 `505243649` is the first appearance to fail attempt 2 as well as attempt 1, see its row) and, as of 2026-09-01, shard 4's (`workspace-folders-refresh.test.tsx`, one test, named with assertion text on its first stream-era appearance), and, as of 2026-09-16, the desktop suite's (`file-token-store.test.ts` > *"subscribe is a live registration that never fires (§4 stub)"* — a test upstream has since DELETED), and, as of 2026-09-18, the dialog twin's (`logs-chooser-dialog.test.tsx` on shard 1 and `about-details-dialog.test.tsx` on shard 3 — same test title, same assertion, same provenance pair, so one shape on two shards; see the `1a2a6a15d` row).
A flake family, not a regression. Prior recorded member: gui-app **shard 1**, on
2026-08-24 (run 32682942738, green on attempt 2) — so gui-app shards have
now flaked at 1, 2, 3 and 4.

## The observability defect that is half of this ticket

**Corrected 2026-08-29 20:15 — it is not "NX collapses the reporter"; it is
Nx's default output style.** With `--tui=false` Nx still buffers the child's
stdout into an internal stream and replays it **after** the failure banner,
**capped** — upstream's own diagnosis, written into its `test.yml` at #951.
Read from `attempts/1/jobs` for every red run in the table (the run's default
`jobs` is the latest attempt, which is all green after a rerun): the shard-3
and shard-4 logs (runs 33147777425, 32999100333, 33122275631) and shard 1's
in run 32682942738 (the matrix entry whose name carries no suffix runs
`--shard=1/4`) end mid-write with no `×` line and no vitest summary — the
signature that reads like a killed process; the two shard-2 logs got as far
as the `❯ file (74 tests | 39 failed)` and `×` lines but are cut before the
assertion text. The darwin job's log is complete both times because the
desktop suite's output is small enough to fit under the cap.

**Fixed on `main` in `3013bdd95` (2026-08-29 20:15 run):** `--outputStyle=stream`
on both `run:` lines, replacing `--tui=false` — the two are mutually
exclusive in Nx (exit 1 before any test runs; verified on this tree's
`nx ^22.7.8`, which is upstream's too). Upstream made the same change in
#951 (`test`, 2026-08-11) and #1552 (darwin, 2026-08-29).

**Mechanism verified 2026-08-30 00:15 on a green pair.** The first Tests run
under the flag (`33248191528` on `b88160082`) was green on attempt 1, so no
red shard has been read under it yet — but its shard-2 job log against the
previous run's green shard-2 log (attempt 2 of `33238440979`, `--tui=false`)
is a controlled pair, and the API's per-line timestamps say *when* each
line reached the log:

| | `--tui=false` (job `99087680293`) | `--outputStyle=stream` (job `99089193089`) |
| --- | --- | --- |
| job log | 688 lines | 1,838 lines |
| vitest per-file lines | **7** | **262** |
| arrival | all seven within 15 ms at 10:26:22Z — replayed after the run | across 170 distinct seconds, 10:36:29Z → 10:40:31Z — live |
| vitest summary | absent | `Test Files 264 passed (264)` / `Tests 2904 passed (2904)` / `Duration 254.00s (… import 170.04s, tests 145.89s …)` |

So the buffered style was dropping 257 of 264 file lines and the summary on
a **green** run — every shard log in the table looked like a killed process
because that is what the old style shows for any gui-app shard. Under
`stream` there is no replay and nothing to cap; a red shard's `×` lines and
assertion text come from the same reporter on the same stream. The red-shard
read is now confirmation, not the proof.

**Where the shards actually run (measured 20:15):** GitHub-hosted 2-core
`ubuntu-latest` — every gui-app job since 08-24 shows `runner_name`
`GitHub Actions N` with label `ubuntu-latest`; `vars.GUI_APP_RUNNER` is
unset (404) and the fork has **zero** self-hosted runners (repo-level,
checked across all of ElliotWood's repos; the AltraCloud org's list is 403
to this token). Upstream runs the same shards on `ubuntu-latest-8-cores`.
So `altra-vm-runner-demo-aue` is **not** what answers these jobs, whatever
else it serves.

## Consequences while open

Red CI on `main` is non-discriminating. Standing practice has been
`gh run rerun --failed` (reruns show as "ElliotWood" — the recorded
attribution quirk), which converts a flake into a green tick and a real
regression into a second identical failure — but only if someone reads
attempt 2, and the ledger has already recorded one red run that no entry
saw.

## Ask

Two independent halves; either alone helps:

1. ✅ **CLOSED 2026-09-01 08:15 — make shard failures observable.**
   Landed in `3013bdd95`; mechanism verified on a green pair 2026-08-30
   00:15 (7 → 262 per-file lines, summary present, lines live). The
   condition this half set for itself — *"confirm the assertion text on the
   next red shard"* — was met by run `33426074181` (shard 4, the `2033ae2ba`
   row): file, full test title, assertion with expected/received, code
   frame and summary all present in the job log. Nothing further to do.
2. **Deflake or quarantine the members** — the two darwin tests
   (`host-management-channel` and `host-lifecycle`, both upstream-inherited
   desktop code, not fork-authored), the named shard-2 member
   (`providers-settings-panel.test.tsx`, the same 39 of 74 both times, green
   locally and on every rerun), the named shard-4 member
   (`workspace-folders-refresh.test.tsx` > *"re-derives on R while the
   picker is open"*, caught 2026-09-01 — upstream-inherited and rewritten
   upstream since, so the fork merge replaces it), and whatever shards 1
   and 3 turn out to be. Note the pattern: every named member so far is
   upstream-inherited and superseded by the pending fork merge — the merge
   is also the deflake.
3. **Decide the runner** — either register `altra-vm-runner-demo-aue` to the
   fork and set `vars.GUI_APP_RUNNER`, or accept 2-core `ubuntu-latest` for a
   suite upstream sizes for 8 cores. The VM is running and this fork's CI
   does not use it. Measured on the green shard 2 of `33248191528`:
   `import 170.04s` of `Duration 254.00s` — two thirds of the shard is
   module import on the 2-core box. Re-measured on the red shard 4 of
   `33426074181`: `import 243.25s` of `Duration 304.29s` — **80%**.

Verify any fix against the runs in the table — same tree, so a rerun of
those exact commits is a controlled experiment.
