# Copy to .env (or export directly) and fill in real values — none of these
# have defaults in source; the bot refuses to start without MicrosoftAppId.

# The bot's Azure App ID. Doubles as: (a) the audience the inbound JWT
# validator requires on every Connector API call, (b) the outbound Connector
# API client id used to authenticate replies back to Bot Service.
MicrosoftAppId=

# The bot's Azure App client secret, for outbound Connector API auth.
MicrosoftAppPassword=

# Required for single-tenant Azure AD app registrations.
MicrosoftAppTenantId=

# Loopback only — see src/config.ts. Public reachability, if chosen, is a
# tunnel (T0a) fronting this port, never this process binding 0.0.0.0.
TEAMS_BOT_HOST=127.0.0.1
TEAMS_BOT_PORT=3978

# R2 groundwork — capture the SHAPE of file attachments arriving from Teams.
#
# Off unless set to exactly "1". With it on, each attachment is logged as its
# keys, types and sizes: `contentType` verbatim (it is the finding), file
# sizes verbatim, URLs as origin plus path depth. Never a file name, never a
# path, never a query string — a Teams `downloadUrl` carries its
# authorisation there, so the query is the difference between a log line and
# a credential.
#
# TEMPORARY. Turn it on, have someone send one file in a personal chat and one
# in a channel, read the two shapes, turn it off. It should not survive R2.
TRAYCER_TEAMS_LOG_RAW_ATTACHMENTS=
