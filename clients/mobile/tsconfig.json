{
  "compilerOptions": {
    "ignoreDeprecations": "6.0",
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "types": ["vitest/globals"],
    "baseUrl": ".",
    "paths": {
      "@traycer-clients/shared/*": ["../shared/*"],
      "@traycer-clients/gui-app": ["../gui-app/index.ts"],
      "@traycer-clients/gui-app/*": ["../gui-app/*"],
      "@traycer-clients/mobile/*": ["./src/*"],
      "@traycer/protocol/utils/*": ["../../protocol/utils/*"],
      "@traycer/protocol/*": ["../../protocol/src/*"],
      "@/*": ["../gui-app/src/*"]
    }
  },
  "include": ["src", "scripts", "capacitor.config.ts", "__tests__"],
  // `src/web/sw.ts` is checked by `tsconfig.sw.json` instead, against the
  // WebWorker lib. Checking it here too is not merely redundant, it is wrong:
  // under this config's DOM lib `self` is a `Window`, so the worker's globals
  // resolve to the wrong types entirely and the file cannot compile.
  "exclude": ["node_modules", "dist", "android", "ios", "web", "src/web/sw.ts"]
}
