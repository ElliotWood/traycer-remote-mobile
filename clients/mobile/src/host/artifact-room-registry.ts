/**
 * MOVED to `@traycer-clients/shared/epic/artifact-room-registry` when the
 * Teams tab needed artifact bodies. Re-exported so no call site moved.
 */
export * from "@traycer-clients/shared/epic/artifact-room-registry";
